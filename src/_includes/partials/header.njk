{% set activePage = page.url %}

<header>

{# informacion de cabecera #}

{# fin informacion de cabecera #}

<div class="row 
            gx-1
            gx-md-3
            text-lg-center
            mb-4
            ">
    
    <div class="col-4
                col-lg-12
                p-3
                ">
      <img class="img-fluid
                  img-thumbnail
                  rounded-3
                  shadow
                  " 
           src="/assets/images/profile-pic-250x250.png" 
           alt="Header Image">
    </div>
    
    <div class="col
                align-self-end
                p-3
                ">
      <h1 class="
                my-0
                h3
                ">
                {{ site.title }}
      </h1>
      <h2 class="
                h5
                mt-0
                
                ">
        {{ site.site_moto }}
      </h2>
      
      <span class="mt-2">
      
      <a class="link-secondary 
                link-offset-2 
                link-underline-opacity-0 
                link-opacity-75-hover
                link-underline-opacity-25-hover
                pe-2
                "
                href="https://github.com" target="_blank"><i class="fa-brands fa-github fa-xl"></i></a>

      <a class="link-secondary 
                link-offset-2 
                link-underline-opacity-0 
                link-opacity-75-hover
                link-underline-opacity-25-hover
                pe-2
                "
                href="https://www.linkedin.com" target="_blank"><i class="fa-brands fa-linkedin  fa-xl"></i></a>

      <a class="link-secondary 
                link-offset-2 
                link-underline-opacity-0 
                link-opacity-75-hover
                link-underline-opacity-25-hover
                pe-2
                "
                href="https://www.instagram.com" target="_blank"><i class="fa-brands fa-instagram  fa-xl"></i></a>

      </span>

        {# <nav class="ep-language-menu">
          <a href="{{ page.url }}" lang="{{ page.lang }}" hreflang="{{ page.lang }}">
            {{ page.lang | upper }}
          </a>

          {% for link in page.url | locale_links %}
          <a href="{{ link.url }}" lang="{{ link.lang }}" hreflang="{{ link.lang }}">
            {{ link.lang | upper }}</a>
          {% endfor %}
        </nav> #}
    
    </div>


        {# <div>
          <nav class="ep-language-menu">
            {% for link in page.url | locale_links %}
            <a class="link-secondary link-underline-opacity-0" href="{{ link.url }}" lang="{{ link.lang }}"
              hreflang="{{ link.lang }}">
              {%- if link.lang == "en" %}
              <img src="/assets/images/uk-icon.png" alt="English">
              {%- endif -%}
              {%- if link.lang == "es" %}
              <img src="/assets/images/sp-icon.png" alt="Español">
              {%- endif -%}
            </a>
            {% endfor %}
          </nav>
        </div> #}

{# PAGE: {{ page | debug }}
 {{ translationKey }} {{ locale }} #}


{%- if locale == 'en' -%}
    {%- set otherLocale = 'es' -%}
{%- else -%}
    {%- set otherLocale = 'en' -%}
{%- endif -%}
{# <pre> PAGE: {{ page | debug }} </pre> </br> #}
{# LOCALE: {{ locale }} </br>
OTHER LOCALE: {{ otherLocale}}  </br>
translation key: {{ translationKey }}  </br> #}


{% set otherLocaleContent = collections.contentByLocale(otherLocale) %}
{# <ul>
  {% for item in otherLocaleContent %}
    <li><a href="{{ item.url }}">{{ item.data.title }} - {{ item.data.locale}}</a></li>
  {% endfor %}
</ul> #}

{%- if translationKey -%}
    {%- for otherContent in otherLocaleContent -%}
        {%- if otherContent.data.translationKey == translationKey -%}
            <p class="is-translation-link noprint">
                <a  href="{{ otherContent.url }}">
                                {%- if otherContent.data.locale == "en" %}
                                <img src="/assets/images/uk-icon.png" alt="English">
                                {%- endif -%}
                                {%- if otherContent.data.locale == "es" %}
                                <img src="/assets/images/sp-icon.png" alt="Español">
                                {%- endif -%}
                  
                  </a>
            </p>
        {%- endif -%}
    {%- endfor -%}
{%- endif -%}


*****************************************</br>



*****************************************</br>

{% set locale = page.lang %}
{% set localizedContent = collections.contentByLocale(locale) %}
<ul>
  {% for item in localizedContent %}
    <li><a href="{{ item.url }}">{{ item.data.title }} - {{ item.data.locale}}</a></li>
  {% endfor %}
</ul>






{# {%- if translationKey -%}
    {%- for otherPost in collections["blog_" + otherLocale] -%}
        {%- if otherPost.data.translationKey == translationKey -%}
            <p >
                <a class="link-secondary link-underline-opacity-0" href="{{ otherPost.url }}">
                  available in: {{otherLocale}}

                  {%- if otherPost.lang == "en" %}
                      <img src="/assets/images/uk-icon.png" alt="English">
                      {%- endif -%}
                      {%- if otherPost.lang == "es" %}
                      <img src="/assets/images/sp-icon.png" alt="Español">
                      {%- endif -%}
                </a>
            </p>
        {%- endif -%}
    {%- endfor -%}
{%- endif -%} #}






                {# <div>
                  <nav class="ep-language-menu">
                    {% for link in page.url | locale_links %}
                    <a class="link-secondary link-underline-opacity-0" href="{{ link.url }}" lang="{{ link.lang }}"
                      hreflang="{{ link.lang }}">
                      {%- if link.lang == "en" %}
                      <img src="/assets/images/uk-icon.png" alt="English">
                      {%- endif -%}
                      {%- if link.lang == "es" %}
                      <img src="/assets/images/sp-icon.png" alt="Español">
                      {%- endif -%}
                    </a>
                    {% endfor %}
                  </nav>
                </div> #}

</div>

</header>